#include <bits/stdc++.h>
using namespace std;

void solve(const int &t)
{
    
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;
    solve(t);
    // while (t--)
    // {
    // }
    return 0;
}